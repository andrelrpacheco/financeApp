import styled from 'styled-components/native'

export const PickerView = styled.View.attrs({
	placeholderTextColor: '#161616'
})`
	margin-top: 20px;
	background-color: rgba(203, 203, 203, 0.5);
	align-items: center;
	width: 90%;
	border-radius: 8px;
	font-size: 16px;
`
